import React from 'react';
import { GridLoader } from 'react-spinners';

export const AuthLoader = (props) => {

  return (
    <GridLoader color="#99bab3" size={15} css={{display: 'block'}}/>
  )
}

